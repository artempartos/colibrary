!!! 5
%html(lang="en")
  %head= render "shared/head"
  %body
    = render "shared/navbar"
    .container
      .row
        .col-lg-12
          = yield
      %footer
        %p &copy; CoLibrary, #{Time.now.year}
