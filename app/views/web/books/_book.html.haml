.col-md-10.blogShort
  %h1= book.title
  = image_tag book.cover.url, class: "pull-left img-responsive thumb margin10 img-thumbnail"
  %em
    TODO: Add Author
    %time= l(book.created_at, format: :long)
  %article
    %p= book.details
  = link_to t('.show'), book_path(book), class: "btn btn-blog pull-right marginBottom10"
  = link_to t('.edit'), edit_book_path(book), class: "btn btn-blog pull-right marginBottom10"
